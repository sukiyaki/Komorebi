=================================================
Komorebi Server Configurator (.NET)
=================================================

Androidアプリ「Komorebi」から、Windows上のEDCB（EpgTimerSrv）録画ファイルに
直接アクセスするためのサーバー側セットアップ支援ツールです。

-------------------------------------------------
1. 概要
-------------------------------------------------
このツールは、以下の作業を自動化します：

(1) シンボリックリンクの作成
Windowsの「mklink /D」コマンドを使用し、EDCBの HttpPublic/video フォルダ内に
録画フォルダへのショートカット（シンボリックリンク）を作成します。

(2) Luaスクリプトの生成
Komorebiアプリが録画パスをURLに変換するためのマッピング定義ファイル
「resolver.lua」を自動生成し、適切な場所に配置します。


-------------------------------------------------
2. 動作要件
-------------------------------------------------
【実行環境 (ユーザー側)】
- OS: Windows 10 / 11 (x64)
- 権限: 管理者権限（シンボリックリンク作成に必要）
- ランタイム: 不要（自己完結型ビルドの場合）

【開発・ビルド環境】
- OS: Mac (Apple Silicon / Intel) または Windows
- SDK: .NET 8.0 SDK
- IDE: Visual Studio Code (Avalonia for VS Code 拡張推奨)


-------------------------------------------------
3. ユーザーガイド（使い方）
-------------------------------------------------

(1) 管理者権限取得
- ユーザーアカウント制御（UAC）のダイアログが表示されるので
  「はい」を選択します。

(2) 基本設定
- 「EDCBの HttpPublic フォルダ」を参照ボタンから選択します。
  （例: C:\EDCB\HttpPublic）

(3) マッピングの追加
- 「録画フォルダ実体」を選択します。
  （例: D:\TV_Rec）
- 「エイリアス名」を入力します（デフォルト: rec）。
- 「リストに追加 / 更新」をクリックします。
- ※複数の物理ドライブを使用している場合は、それぞれ別のエイリアス名で
  追加してください。
  ※注意（ネットワークドライブをご利用の方へ）
  録画フォルダをネットワークドライブ（Z:\ など）に割り当てている場合、「参照」ボタンを押すとWindowsの権限の仕様によりアプリが終了してしまうことがあります。その場合は、参照ボタンを使わずにテキストボックスへ直接パス（例：Z:\TV_Rec）を入力してください。

(4) セットアップの実行
- 「セットアップ実行」ボタンをクリックします。
- 完了メッセージが出れば成功です。


-------------------------------------------------
4. 開発者ガイド（ビルド方法）
-------------------------------------------------
Mac環境からWindows用のシングルバイナリ（.exe）を出力する方法です。

[プロジェクトの復元]
dotnet restore

[Windows用バイナリの出力 (自己完結型・トリミング有効)]
dotnet publish -c Release -r win-x64 \
  --self-contained true \
  -p:PublishSingleFile=true \
  -p:IncludeNativeLibrariesForSelfExtract=true \
  -p:PublishTrimmed=true

- 出力先: bin/Release/net8.0/win-x64/publish/KomorebiConfigurator.exe
- ファイルサイズ: 約44MB（トリミング有効時）


-------------------------------------------------
5. 技術仕様
-------------------------------------------------
【配置ファイル】
実行後、以下のディレクトリ構造が構築されます。

HttpPublic/
├── video/
│   └── [エイリアス名] (D:\TV_Rec 等へのシンボリックリンク)
└── komorebi/
    └── resolver.lua (自動生成されたパス変換スクリプト)

【マッピングの仕組み】
resolver.lua 内の MAPPING テーブルによって、
EDCBが返す物理パスとHTTP公開用パスの紐付け管理が行われます。

例：
local MAPPING = {
    ["D:\\TV_Rec"] = "rec",
    ["E:\\Anime"] = "anime",
}

【開発時の注意点 (Mac)】
- UIフレームワークとして Avalonia UI を使用しているため、
  Mac上でも dotnet run で画面のプレビューおよびデバッグが可能です。
- ただし、Windows OS固有の mklink コマンドの実行テストは、
  Windows実機または仮想環境が必要です。


-------------------------------------------------
6. 変更履歴
-------------------------------------------------
- v5.0.0
  * resolver.lua の配置場所を HttpPublic/komorebi/ 配下に変更。
  * シンボリックリンクの作成場所を HttpPublic/video/ 配下に変更。
  * 生成される Lua スクリプトの互換性を向上（EDCB-Wine環境や古い util.lua 環境での JSON エンコードエラーおよび未定義関数エラーを回避するためのセーフティロジックを導入）。
  * 放送波切替リストのUI対応に伴う内部テンプレートの更新。

- v4.0.0
  * 初版リリース。
  * Avalonia UI によるクロスプラットフォーム開発対応。
  * 自己完結型シングルバイナリビルド設定の導入。

-------------------------------------------------
7. ライセンス
-------------------------------------------------
このプロジェクトは Komorebi プロジェクトの一部として開発されています。
